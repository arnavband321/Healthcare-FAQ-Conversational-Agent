// MediAssist Frontend Configuration
// When deploying frontend and backend to separate domains:
// Set window.API_BASE_URL to your backend URL (e.g., "https://your-backend.onrender.com")
// If left empty (""), the frontend will check localStorage or fall back to the current origin.
window.API_BASE_URL = window.API_BASE_URL || "https://healthcare-faq-conversational-agent-hp1y.onrender.com";
